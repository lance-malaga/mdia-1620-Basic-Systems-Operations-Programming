// single line

/**
 * multiple line
 * another line
 */

var num = myFunction(2, 3);

/**
 * @desc This will calculate the area
 * @param {*} x is the width
 * @param {*} y is the height
 * @returns the area of the object
 */
function myFunction(x, y){
    return x * y;
}

console.log(num);

// hello my name is lance